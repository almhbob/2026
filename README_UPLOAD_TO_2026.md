# RemoteAssist APK build package for repository almhbob/2026

Upload these contents to the root of `almhbob/2026`.

After upload, GitHub Actions will show a workflow named `Build RemoteAssist APK`.
Run it or push changes, then download the artifact named `RemoteAssist-debug-apk`.

The APK output path inside the artifact will be:

```text
RemoteAssist/android/app/build/outputs/apk/debug/app-debug.apk
```
